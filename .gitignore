# Python
__pycache__/
*.pyc

# Local-only preview config (used by Claude Code's preview, not needed on GitHub)
.claude/

# macOS
.DS_Store

# Generated site — rebuilt from data/ on every run and served from the GitHub
# Pages artifact, so it is never committed to the repo.
site/
